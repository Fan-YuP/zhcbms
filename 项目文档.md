# 企业贷款综合融资成本公示系统 — 项目文档

> 版本：v1.0  
> 更新日期：2026-08-13  
> 适用范围：企业贷款综合融资成本年化率测算与公示

---

## 目录

- [第一部分 程序安装部署须知](#第一部分-程序安装部署须知)
  - [1.1 系统概述](#11-系统概述)
  - [1.2 环境要求](#12-环境要求)
  - [1.3 安装步骤](#13-安装步骤)
  - [1.4 部署流程](#14-部署流程)
  - [1.5 常见部署问题及解决方案](#15-常见部署问题及解决方案)
- [第二部分 使用手册](#第二部分-使用手册)
  - [2.1 系统访问](#21-系统访问)
  - [2.2 界面总览](#22-界面总览)
  - [2.3 贷款基本信息配置](#23-贷款基本信息配置)
  - [2.4 融资费用明细配置](#24-融资费用明细配置)
  - [2.5 综合融资成本测算](#25-综合融资成本测算)
  - [2.6 公示结果查看](#26-公示结果查看)
  - [2.7 PDF 导出与打印](#27-pdf-导出与打印)
  - [2.8 使用示例](#28-使用示例)
  - [2.9 常见操作问题排查](#29-常见操作问题排查)
- [第三部分 计算规则](#第三部分-计算规则)
  - [3.1 规则总览](#31-规则总览)
  - [3.2 贷款年化利率计算](#32-贷款年化利率计算)
  - [3.3 费用年化率计算](#33-费用年化率计算)
  - [3.4 单位周期与名义月差](#34-单位周期与名义月差)
  - [3.5 还款计划生成](#35-还款计划生成)
  - [3.6 现金流构造](#36-现金流构造)
  - [3.7 IRR 求解算法](#37-irr-求解算法)
  - [3.8 单利年化换算](#38-单利年化换算)
  - [3.9 循环授信模式](#39-循环授信模式)
  - [3.10 边界条件处理](#310-边界条件处理)
  - [3.11 计算结果验证方法](#311-计算结果验证方法)

---

# 第一部分 程序安装部署须知

## 1.1 系统概述

本系统是一个**纯前端单页应用（SPA）**，无需后端服务器、无需数据库、无需安装任何运行时环境（除 HTTP 服务组件外）。所有计算逻辑在浏览器端完成，用户数据不涉及上传或持久化。

### 技术栈

| 组件 | 版本 | 用途 |
|------|------|------|
| HTML5 + CSS3 + JavaScript (ES6+) | — | 页面结构、样式、业务逻辑 |
| [html2canvas](https://github.com/niklasvh/html2canvas) | 1.4.1 | 将公示面板 DOM 渲染为 Canvas 图像 |
| [jsPDF](https://github.com/parallax/jsPDF) | 2.5.1 | 将 Canvas 图像打包生成 PDF 文件 |

### 项目目录结构

```
综合融资成本明示/
├── index.html                     # 主页面（包含全部计算逻辑与界面）
├── lib/                           # 第三方库（本地副本，无需外网）
│   ├── html2canvas.min.js         # DOM 截图引擎  (106 KB)
│   └── jspdf.umd.min.js           # PDF 生成引擎   (364 KB)
└── 项目文档.md                    # 本文档
```

> ⚠️ 部署时**必须保留 `lib/` 子目录**，否则 PDF 导出功能无法使用。

---

## 1.2 环境要求

### 客户端（使用方）

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 7/8/10/11、macOS 10.12+、主流 Linux 发行版 |
| 浏览器 | Chrome 90+、Edge 90+、Firefox 88+、Safari 14+（推荐 Chrome/Edge） |
| 屏幕分辨率 | 最低 1280×720，推荐 1920×1080 及以上 |
| 内存 | 建议 4GB 及以上（PDF 导出时需要内存进行 Canvas 渲染） |
| 网络 | **内网部署场景无需外网连接**；首次部署到内网机器时需外网下载依赖 |

### 服务端（部署方）

| 项目 | 最低要求 | 推荐配置 |
|------|----------|----------|
| 操作系统 | Windows Server 2016 / Linux (CentOS 7+) | Windows Server 2019+ / Ubuntu 20.04+ |
| CPU | 1 核 | 2 核及以上 |
| 内存 | 512 MB | 1 GB 及以上 |
| 磁盘 | 20 MB | 100 MB |
| HTTP 服务器 | Python 3.x（内置 http.server）| Nginx / IIS / Apache |
| Python（可选） | 3.6+ | 3.9+ |

---

## 1.3 安装步骤

### 方式 A：从现有项目直接复制（推荐）

项目已是可运行的成品，无需构建或编译：

```
1. 将整个「综合融资成本明示」文件夹完整复制到目标服务器
   （确保 lib/ 子目录也一并复制）

2. 确认目录结构：
   综合融资成本明示/
   ├── index.html
   └── lib/
       ├── html2canvas.min.js
       └── jspdf.umd.min.js

3. 直接部署，无需额外安装步骤
```

### 方式 B：从零下载（需要外网）

如果目标服务器无法直接复制文件，需要手动下载依赖：

**步骤 1**：下载主页面 `index.html`（从项目仓库或已有部署拷贝）。

**步骤 2**：下载第三方库到 `lib/` 目录：

```bash
mkdir lib

# 下载 html2canvas v1.4.1
curl -L -o lib/html2canvas.min.js \
  https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js

# 下载 jsPDF v2.5.1
curl -L -o lib/jspdf.umd.min.js \
  https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js
```

**步骤 3**：校验文件完整性：

```bash
# 应输出约 106496 字节
ls -la lib/html2canvas.min.js

# 应输出约 364463 字节
ls -la lib/jspdf.umd.min.js
```

**步骤 4**：确保 [index.html 第 6-7 行](file:///f:/综合融资成本明示/index.html#L6-L7) 的引用路径正确：

```html
<script src="lib/html2canvas.min.js"></script>
<script src="lib/jspdf.umd.min.js"></script>
```

---

## 1.4 部署流程

### 开发 / 测试环境（Python http.server）

适合本地开发、临时测试、小范围内部使用：

```bash
# 进入项目目录
cd f:\综合融资成本明示

# 启动 HTTP 服务（绑定所有网卡，局域网可访问）
python -m http.server 8080 --bind 0.0.0.0

# 或者简写（默认绑定 127.0.0.1，仅本机访问）
python -m http.server 8080
```

启动成功后控制台会显示：
```
Serving HTTP on 0.0.0.0 port 8080 ...
```

**局域网内其他电脑访问**：
- http://<服务器IP>:8080/index.html
- 例如：http://10.6.4.124:8080/index.html

### 生产环境（Nginx）

适合正式环境、高并发场景：

```nginx
# /etc/nginx/conf.d/loan-cost.conf
server {
    listen       80;
    server_name  loan-cost.internal.company.com;

    root /var/www/loan-cost;
    index index.html;

    # SPA 单页应用：所有路径都返回 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}
```

### 生产环境（Windows IIS）

1. 打开 **IIS 管理器** → **站点** → **添加网站**
2. 配置：
   - 物理路径：指向 `综合融资成本明示` 文件夹
   - 端口：8080（或任意空闲端口）
   - 主机名：可留空（直接用 IP 访问）
3. 确保已安装 **URL Rewrite** 模块（可选，用于 SPA 路由）
4. 添加 MIME 类型：`.js → application/javascript`、`.html → text/html`

### 服务启动与停止命令速查

| 环境 | 启动 | 停止 | 检查状态 |
|------|------|------|----------|
| Python http.server | `python -m http.server 8080 --bind 0.0.0.0` | Ctrl+C | — |
| Nginx | `nginx` | `nginx -s stop` | `nginx -t` |
| IIS | `iisreset /start` | `iisreset /stop` | `iisreset /status` |

---

## 1.5 常见部署问题及解决方案

### Q1：浏览器访问页面空白 / 404

**可能原因**：
- `index.html` 文件名大小写不匹配（Linux 区分大小写）
- 服务端口被防火墙拦截
- HTTP 服务器未正确启动

**排查步骤**：
```bash
# 1. 检查 HTTP 服务是否在运行
netstat -tlnp | grep 8080     # Linux
netstat -ano | findstr 8080   # Windows

# 2. 本机 curl 测试
curl http://127.0.0.1:8080/index.html | head -5

# 3. 检查 index.html 是否存在
ls -la index.html
```

### Q2：PDF 导出失败 / 导出空白 PDF

**可能原因**：
- `lib/` 目录未正确部署
- 浏览器禁用了 Canvas 或 `toDataURL`
- CORS 安全策略（但本项目是同源，一般不会触发）

**排查步骤**：
1. 打开浏览器 **开发者工具（F12）** → **Console** 面板
2. 刷新页面，检查是否有红色报错
3. 检查 **Network** 面板中 `lib/html2canvas.min.js` 和 `lib/jspdf.umd.min.js` 是否 200 状态
4. 检查文件 URL 是否能直接访问：
   - 浏览器直接打开 `http://服务器IP:8080/lib/html2canvas.min.js`
   - 应该能看到一大段 JavaScript 代码

### Q3：其他电脑无法访问（ping 通但浏览器打不开）

**可能原因**：Windows 防火墙拦截

**解决方案**（管理员 PowerShell 执行）：
```powershell
netsh advfirewall firewall add rule `
  name="综合融资成本公示系统" `
  dir=in action=allow protocol=TCP localport=8080
```

### Q4：页面加载极慢

**可能原因**：
- 网络带宽低
- 服务器磁盘 I/O 瓶颈

**解决方案**：
- 确认局域网带宽 ≥ 100 Mbps
- 检查 `lib/jspdf.umd.min.js` 是否被正确缓存（首次加载后应走浏览器缓存）
- 推荐使用 Nginx/IIS 等专业 HTTP 服务器（比 Python http.server 性能高 10 倍以上）

### Q5：文件路径中的中文导致 404

Python http.server 在某些 Python 版本上处理中文路径有 bug。**避免把项目放在含中文的路径下**，或使用 Nginx/IIS 等成熟的服务器软件。

---

# 第二部分 使用手册

## 2.1 系统访问

### 访问地址

部署完成后，通过浏览器打开：

```
http://<服务器IP>:<端口>/index.html
```

### 浏览器兼容性

| 浏览器 | 最低版本 | 推荐版本 | 备注 |
|--------|----------|----------|------|
| Google Chrome | 90 | 最新稳定版 | 首选推荐 |
| Microsoft Edge | 90 | 最新稳定版（Chromium 内核） | Windows 自带 |
| Mozilla Firefox | 88 | 最新稳定版 | 可用 |
| Safari | 14 | 16+ | macOS/iOS 用户可用 |
| 微信内置浏览器 | — | — | ⚠️ 不推荐，PDF 下载可能被拦截 |

> ⚠️ **首次使用建议**：在 Chrome/Edge 中打开后按 **Ctrl+D** 收藏，方便下次访问。

---

## 2.2 界面总览

页面分为三个主要区域：

```
┌─────────────────────────────────────────────────────────┐
│  企业贷款综合融资成本公示系统           ← 页面标题       │
├─────────────────────────────────────────────────────────┤
│  一、贷款基本信息                        ← 输入区 1     │
│  ┌─────────┬─────────┬─────────┬─────────┬─────────┐   │
│  │本金      │年利率    │还款方式  │起始日    │到期日    │   │
│  │还款频率  │首次还款日│循环授信  │         │         │   │
│  └─────────┴─────────┴─────────┴─────────┴─────────┘   │
├─────────────────────────────────────────────────────────┤
│  二、融资费用明细配置                    ← 输入区 2     │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 费用项 #1 [期初一次性]    [删除]                  │   │
│  │ 费用名称: ________  费用金额: ________            │   │
│  │ ─────────────────────────────────────────────── │   │
│  │ 费用项 #2 [周期性支付]    [删除]                  │   │
│  │ 费用名称: ________  每期费用: ____  期数: __     │   │
│  │ 支付时机: [期初/期末]                              │   │
│  └─────────────────────────────────────────────────┘   │
│  [+ 添加期初一次性费用]  [+ 添加周期性费用]              │
├─────────────────────────────────────────────────────────┤
│       [综合融资成本测算]                 ← 计算按钮    │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │  公示结果区（测算前隐藏）                           │   │
│  │  ┌─────────────────────────────────────────┐    │   │
│  │  │  综合融资成本年化率  9.15%                │    │   │
│  │  └─────────────────────────────────────────┘    │   │
│  │  三、贷款基本信息公示                              │   │
│  │  四、还款计划表（≤60期显示）                       │   │
│  │  五、融资费用明细及年化率分解                       │   │
│  │  六、综合融资成本汇总                              │   │
│  │  [导出为PDF] [打印]                               │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

---

## 2.3 贷款基本信息配置

### 字段说明

| 字段 | 类型 | 默认值 | 必填 | 说明 |
|------|------|--------|------|------|
| 贷款本金 | 数字 | 1,000,000 | ✅ | 单位：元，支持输入任意正数 |
| 名义年利率 | 数字 | 4.75 | ✅ | 单位：%，例如 4.75 表示 4.75% |
| 还款方式 | 下拉框 | 等额本息 | ✅ | 见下方还款方式说明 |
| 起始日 | 日期 | 2024-01-01 | ✅ | 贷款实际放款日期 |
| 到期日 | 日期 | 2026-01-01 | ✅ | 贷款最后还款日 |
| 还款频率 | 下拉框 | 按月 | ✅ | 决定单位周期长度 |
| 首次还款日 | 日期 | （可选） | ⚠️ | 留空则自动起始日 + 1 周期 |
| 循环授信模式 | 下拉框 | 否 | ✅ | 见下方说明 |

### 还款方式说明

| 还款方式 | 每期还款额 | 每期本金 | 每期利息 | 适用场景 |
|----------|-----------|---------|---------|----------|
| **到期一次性还本** | 利息（周期内） | 0（最后一期还全部本金） | 本金 × 周期利率 | 短期周转贷、委贷等 |
| **等额本金** | 递减 | 固定 = 本金/期数 | 剩余本金 × 周期利率 | 希望总利息最少 |
| **等额本息** | 固定 | 递增 | 递减 | 月供稳定、最常用 |

### 循环授信模式

设置为"是"时，费用年化率计算以**授信额度和授信期限**为基准，而非每次提款的具体金额和期限。对应央行文档"（三）一次授信循环使用额度"章节的规则。

---

## 2.4 融资费用明细配置

### 费用类型

| 类型 | 说明 | 典型场景 |
|------|------|----------|
| **期初一次性费用** | 贷款发放前/发放初期一次性付清 | 担保费、评估费、公证费、过桥费、中介服务费、供应链平台服务费 |
| **周期性支付费用** | 随还款周期分期支付 | 担保费（按年/月收）、保证保险费、银团代理费、动产质押监管费 |

### 期初一次性费用配置项

| 字段 | 说明 |
|------|------|
| 费用名称 | 自由输入，如"融资担保费"、"抵押评估费" |
| 费用金额 | 一次性支付的总金额（元） |

### 周期性支付费用配置项

| 字段 | 说明 |
|------|------|
| 费用名称 | 自由输入，如"账户管理费"、"银团代理费" |
| 每期费用 | 每个周期支付的金额（元） |
| 支付期数 | 共支付多少期 |
| 支付时机 | **期初支付** = 每期开始时支付；**期末支付** = 每期结束时支付 |

### 操作按钮

| 按钮 | 功能 |
|------|------|
| **+ 添加期初一次性费用** | 新增一个期初费用项 |
| **+ 添加周期性费用** | 新增一个周期费用项 |
| **× 删除**（费用项右上角） | 删除该费用项 |

> 💡 **小提示**：费用项可以任意增删，没有数量限制。系统会将所有费用项的年化率逐项加总到综合融资成本中。

---

## 2.5 综合融资成本测算

点击 **"综合融资成本测算"** 按钮触发计算。系统会：

1. 校验输入参数是否完整
2. 生成还款计划（每期本金、利息、总还款）
3. 对贷款利息计算真实年化利率（IRR 法）
4. 对每一项费用分别计算年化率（IRR 法 + 单利换算）
5. 加总得到综合融资成本年化率
6. 渲染公示面板

### 计算耗时参考

| 场景 | 期数 | 费用项 | 估算耗时 |
|------|------|--------|----------|
| 按月 2 年 | 24 期 | 1 项 | < 100ms |
| 按月 5 年 | 60 期 | 3 项 | < 200ms |
| 按年 10 年 | 10 期 | 5 项 | < 500ms |

系统采用二分法求解 IRR（最多 300 次迭代），任何场景下均可在 1 秒内返回结果。

---

## 2.6 公示结果查看

测算完成后，公示面板自动滚动到视野内，包含以下区块：

### 一、综合融资成本年化率高亮卡

页面核心，以大号蓝字显示：

```
┌────────────────────────────────────┐
│      综合融资成本年化率               │
│                                    │
│           9.1515%                   │  ← 核心数值
│                                    │
│   贷款利息年化率 4.5922% +          │
│   费用年化率合计 4.5592%            │  ← 分解展示
│                                    │
│ ┌────────┬────────┬────────┐       │
│ │ 本金    │ 期数   │ 费用项  │       │  ← 速览指标
│ │ 100万  │ 24    │ 1      │       │
│ └────────┴────────┴────────┘       │
└────────────────────────────────────┘
```

### 二、贷款基本信息公示

以 4 列网格展示输入参数，便于快速核对。

### 三、还款计划表（≤60 期时显示）

| 列 | 说明 |
|----|------|
| 期数 | 1, 2, 3, ... N |
| 还款日期 | 每期还款日（实际日期） |
| 本金（元） | 该期偿还本金部分 |
| 利息（元） | 该期偿还利息部分 |
| 合计（元） | = 本金 + 利息 |

> ⚠️ **超过 60 期**时自动隐藏还款计划表（避免页面过长）。

### 四、融资费用明细及年化率分解

| 列 | 说明 |
|----|------|
| 序号 | 1, 2, ... |
| 费用名称 | 用户配置的名称 |
| 费用类型 | 期初一次性 / 周期性支付 |
| 费用总额（元） | 该项费用总支付金额 |
| 单位周期费率 | IRR 法求得的周期费率（精确到 6 位小数） |
| **年化率** | 单利换算后的年化率（**核心结果**） |
| 占综合成本比重 | 该项费用 / 综合融资成本 |

### 五、综合融资成本汇总

| 行 | 说明 |
|----|------|
| 贷款利息年化率 | 单独列出 |
| 各项费用年化率 | 按费用项逐项列出 |
| **费用年化率合计** | 所有费用项加总（小计行） |
| **综合融资成本年化率** | 利息 + 费用合计（总计行，加粗） |

---

## 2.7 PDF 导出与打印

### PDF 导出

点击 **"导出为PDF"** 按钮：

1. 系统自动克隆公示面板 DOM
2. 清理打印无关元素、修复滚动容器样式
3. 将 DOM 渲染为高分辨率 Canvas
4. 按 A4 纸张尺寸切片打包成多页 PDF
5. 自动开始下载

**文件名格式**：`企业贷款综合融资成本公示_YYYY-MM-DD.pdf`

**PDF 规格**：
- 纸张尺寸：A4（210mm × 297mm）
- 页边距：10mm
- 图像质量：JPEG 95%，2× 缩放（高清）
- 支持多页自动分页

### 浏览器打印

点击 **"打印"** 按钮，调用浏览器原生打印对话框。打印时：
- 页面顶部的输入表单和按钮区自动隐藏（CSS `@media print` 规则 + `.no-print` 类）
- 公示面板自动扩展，`max-height`/`overflow` 限制解除
- 适合打印存档或转 PDF（在打印对话框选"另存为 PDF"）

### 导出异常处理

| 现象 | 可能原因 | 解决方法 |
|------|----------|----------|
| 点按钮无反应 | 未先执行测算 | 先点"综合融资成本测算"生成内容 |
| 下载的 PDF 只有表头无内容 | 浏览器版本过低 | 升级到 Chrome 90+ / Edge 90+ |
| PDF 空白页 | 内存不足 | 关闭其他程序、刷新页面重试 |
| 中文显示为方框 | 本地无对应字体 | 系统安装宋体/微软雅黑/黑体 |

---

## 2.8 使用示例

### 示例 1：等额本金 + 期初担保费（央行文档例3）

**场景**：某贷款产品，本金 100 万元，期限 2 年，等额本金还款，按月，共 24 期。借款人在提款日按本金 1% 支付保证保险费 1 万元。

| 步骤 | 操作 | 值 |
|------|------|----|
| 1 | 贷款本金 | `1000000` |
| 2 | 名义年利率 | `4.75` |
| 3 | 还款方式 | **等额本金** |
| 4 | 起始日 | `2024-01-01` |
| 5 | 到期日 | `2026-01-01` |
| 6 | 还款频率 | **按月** |
| 7 | 首次还款日 | （留空） |
| 8 | 循环授信模式 | **否** |
| 9 | 删除默认周期性费用项 | — |
| 10 | 费用项 #1：点击"添加期初一次性费用" | — |
| 11 | 费用名称 | `保证保险费` |
| 12 | 费用金额 | `10000` |
| 13 | 点击 **综合融资成本测算** | — |

**预期结果**：
- 保证保险费年化率 ≈ **0.967%**
- 综合融资成本年化率 ≈ **5.559%**（利息 4.592% + 保证保险费 0.967%）

### 示例 2：等额本息非整数周期 + 周期代理费（央行文档例12）

**场景**：某企业借银团贷款 100 万元，期限 24 个月，2024 年 1 月 10 日放款，还款从 2024 年 2 月 20 日开始，等额本息偿付，共 24 期。需按月支付银团贷款代理费，月费率 0.2%。

| 步骤 | 操作 | 值 |
|------|------|----|
| 1 | 还款方式 | **等额本息** |
| 2 | 起始日 | `2024-01-10` |
| 3 | 到期日 | `2026-01-10` |
| 4 | 首次还款日 | `2024-02-20` ⚠️ **关键！** |
| 5 | 删除默认期初费用 | — |
| 6 | 费用项 #1（周期性）：费用名称 | `银团贷款代理费` |
| 7 | 每期费用 | `2000` |
| 8 | 支付期数 | `24` |
| 9 | 支付时机 | **期末支付** |
| 10 | 点击测算 | — |

**预期结果**：
- 银团贷款代理费年化率 ≈ **4.369%**

### 示例 3：循环授信 + 期初担保费（央行文档例13）

**场景**：银行向某企业授信 100 万元，授信期限 2 年，在授信日一次性支付担保费 1 万元。授信期内可随时提款和提前还款。

| 步骤 | 操作 | 值 |
|------|------|----|
| 1 | 还款方式 | **到期一次性还本** |
| 2 | 起始日 | `2024-01-01` |
| 3 | 到期日 | `2026-01-01` |
| 4 | 循环授信模式 | **是** ⚠️ **关键！** |
| 5 | 删除默认周期性费用，保留期初费用 | — |
| 6 | 期初费用名称 | `担保费` |
| 7 | 期初费用金额 | `10000` |
| 8 | 点击测算 | — |

**预期结果**：
- 担保费年化率 ≈ **0.50%**

---

## 2.9 常见操作问题排查

### FAQ 1：测算后修改了参数，结果没变？

点击参数修改后，**需要重新点击"综合融资成本测算"按钮**。系统不会自动监听输入变化。

### FAQ 2：费用项删除后年化率没减少？

同 FAQ 1，删完费用项后重新点击测算按钮。

### FAQ 3：输入框里的金额能加千分位逗号吗？

不建议。`1,000,000` 会被解析为 `1`。直接输入纯数字：`1000000`。

### FAQ 4：年利率 4.75 还是 0.0475？

输入 **4.75**（百分比表示）。系统内部会自动除以 100。

### FAQ 5：还款计划表没显示？

当还款总期数超过 60 期时，系统自动隐藏还款计划表以避免页面过长。不影响费用年化率和综合成本的计算精度。

### FAQ 6：测算结果和央行文档示例有微小偏差（< 0.01%）？

正常现象。可能源于：
- 利率精度：央行文档示例部分隐含了 4.747% 等非整数利率
- 小数舍入：各环节保留小数位不同
- 单利/复利换算：本系统严格执行单利换算规则

偏差在 ±0.01% 内均属正常。

---

# 第三部分 计算规则

> ⚠️ 本章节详细阐述系统核心数学逻辑，供技术人员核对实现精度、二次开发、或向审计方解释测算依据时参考。所有规则均参考《中国人民银行公告〔2021〕第 3 号》及配套的《企业贷款综合融资成本年化率计算规则及示例》。

## 3.1 规则总览

**综合融资成本年化率** = 贷款利息年化率 + Σ（各项费用年化率）

其中：
- 贷款利息年化率：IRR 法 + 单利换算
- 费用年化率（每一项）：IRR 法 + 单利换算，**逐项独立计算后加总**

### 核心计算流程

```
输入: 本金 A, 年利率 r, 期限 T, 还款方式, 频率 n, 起始/到期日, 费用项列表
      │
      ▼
┌─────────────────────────┐
│  Step 1: 生成还款计划    │  → 每期还款日期、本金、利息
│  - 计算总期数 N          │  → 每期周期偏移 offset (st + ft)
│  - 计算每期还款额         │
└─────────────────────────┘
      │
      ▼
┌─────────────────────────┐
│  Step 2: 构造现金流      │  → 本金流入: [0, +A]
│  - 贷款利息 CF           │  → 本息流出: [offset_t, -(P_t + I_t)]
└─────────────────────────┘
      │
      ▼
┌─────────────────────────┐
│  Step 3: IRR 求解        │  → 二分法求解: Σ CF_t / (1 + R)^offset_t = 0
└─────────────────────────┘
      │
      ▼
┌─────────────────────────┐
│  Step 4: 单利换算        │  → 年化率 = R × n (n = 年内期数)
└─────────────────────────┘
      │
      ▼
┌─────────────────────────┐
│  Step 5: 逐项加总        │  → 综合融资成本年化率 = 利息率 + Σ 费用率
└─────────────────────────┘
```

---

## 3.2 贷款年化利率计算

贷款利息年化利率的计算遵循《中国人民银行公告〔2021〕第 3 号》中的 IRR 法。

### 现金流构造

```
本金流入:  t=0,  amt = +A        （贷款发放）
本息流出:  t=st + ft,  amt = -(P_t + I_t)   （每期偿还）
```

其中：
- `A` = 贷款本金
- `P_t` = 第 t 期偿还本金
- `I_t` = 第 t 期偿还利息
- `st + ft` = 起始日到第 t 次还款日的名义周期偏移（整数部分 + 小数部分）

### IRR 方程

$$
A = \sum_{t=1}^{N} \frac{P_t + I_t}{(1 + R_{\text{rate}})^{s_t + f_t}}
$$

### 年化率

$$
\text{年利率} = R_{\text{rate}} \times n
$$

其中 `n` 为年内还款频率（按月 n=12，按季 n=4，按年 n=1）。

---

## 3.3 费用年化率计算

每一项费用**独立计算年化率**，然后加总。费用类型分为两大类。

### 3.3.1 期初一次性费用

现金流构造：
```
本金流入:  t=0,  amt = +A
费用流出:  t=0,  amt = -F      （贷款发放同时支付费用）
本金流出:  t=offset_t,  amt = -P_t   （每期偿还本金，与利息计算中的本金序列相同）
```

IRR 方程：
$$
A + F = \sum_{t=1}^{N} \frac{P_t}{(1 + R_j)^{s_t + f_t}}
$$

移项后等价于：
$$
A = \sum_{t=1}^{N} \frac{P_t}{(1 + R_j)^{s_t + f_t}} + \frac{F}{(1 + R_j)^0}
$$

### 3.3.2 周期性支付费用（期初支付）

期初支付 = 每期开始时支付费用。费用现金流 offset 为 `0, 1, 2, ..., periods-1`：

```
本金流入:  t=0,  amt = +A
费用流出:  t=0,  amt = -F_per
费用流出:  t=1,  amt = -F_per
...
费用流出:  t=periods-1,  amt = -F_per
本金流出:  t=offset_t,  amt = -P_t   （每期偿还本金）
```

### 3.3.3 周期性支付费用（期末支付）

期末支付 = 每期结束时支付费用。费用现金流与本金同 offset：

```
本金流入:  t=0,  amt = +A
本金流出+费用流出:  t=offset_t,  amt = -(P_t + F_per)   （费用与本金同次支付）
```

> 💡 **关键**：费用年化率计算中，**本金序列来自标准整数周期年金公式**，与实际还款日的非整数 offset 解耦。这样才能与央行文档示例对齐（如例6、例12）。

---

## 3.4 单位周期与名义月差

### 单位周期（Unit Period）

出现次数最多的相邻两次计息之间的日期间隔，最长不超过 1 年。在单次提款、单次付款的情形中，单位周期为整个期限（但不超过 1 年）。

| 还款频率 | 单位周期 | 单位天数 | 年内期数 n |
|----------|----------|----------|-----------|
| 按月 | 1 个月 | 30 天 | 12 |
| 按季 | 1 季度 | 90 天 | 4 |
| 按年 | 1 年 | 365 天 | 1 |

### 名义月差（Nominal Month Integer）

本系统采用**名义月份差**（30/360 体系），而非实际日历天数：

```
名义月差 = (年份差 × 12 + 月份差) + 日期差 / 30
```

示例：
| 起始日 | 结束日 | 名义月差 |
|--------|--------|----------|
| 2024-01-01 | 2024-02-01 | 1 + 0/30 = **1.0000** |
| 2024-01-10 | 2024-02-20 | 1 + 10/30 = **1.3333** |
| 2023-05-30 | 2025-03-17（按年） | 22/12 + 17/365 ≈ **1.6109** |

### 周期偏移 offset (st + ft)

对于**非一次性还本**的还款方式：
- 如果首次还款日 = 起始日 + 1 周期（默认）：offset = 1, 2, 3, ..., N
- 如果首次还款日提前/延后：offset = firstOffset + t, 其中 firstOffset 为起始日到首次还款日的名义周期差
- 最后一期强制 offset = N

对于**一次性还本**：offset = 起始日到到期日的名义周期差（可能带小数）

---

## 3.5 还款计划生成

### 3.5.1 等额本息

每期还款额 C 由年金公式给出：

$$
C = A \cdot \frac{r(1+r)^N}{(1+r)^N - 1}
$$

其中 `r = 年利率 / n` 为周期利率，`N` 为总期数。

每期本金偿还额（标准序列）：

$$
P_t = C - (\text{剩余本金}) \times r
$$

> ⚠️ **重要**：费用年化率计算中使用的 `P_t` 序列来自此标准公式，**不受实际还款日影响**。每期利息则根据实际相邻两次还款日的名义月差独立计算。

### 3.5.2 等额本金

每期固定偿还本金：

$$
P_t = \frac{A}{N}
$$

每期利息 = 剩余本金 × 周期利率 × 名义月差（实际间隔）

### 3.5.3 到期一次性还本

最后一期偿还全部本金 `A`；之前各期仅偿还利息。

---

## 3.6 现金流构造

系统采用以下通用现金流数据结构：

```javascript
{
  offset: Number,    // 起始日到支付日的名义周期偏移 (st + ft)
  amt:   Number      // 现金流金额（流入为正，流出为负）
}
```

### 贷款利息现金流

```javascript
const cashflows = [
  { offset: 0,                amt: +principal },   // 本金流入
  ...schedule.map(r => ({
    offset: r.offset,
    amt: -(r.principal + r.interest)                 // 每期本息流出
  }))
];
```

### 期初一次性费用现金流

```javascript
const feeCashflows = [
  { offset: 0,                amt: +principal },   // 本金流入
  { offset: 0,                amt: -feeAmount },   // 费用流出
  ...principals.map(p => ({
    offset: correspondingOffset,
    amt: -p                                         // 本金流出
  }))
];
```

### 周期性费用（期末支付）现金流

```javascript
const feeCashflows = [
  { offset: 0,                amt: +principal },   // 本金流入
  ...mergedPrincipalsFees.map(item => ({
    offset: item.offset,
    amt: -(item.principal + item.feePerPeriod)      // 费用与本金同次流出
  }))
];
```

### 排序

所有现金流按 `offset` 升序排列后送入 IRR 求解器。

---

## 3.7 IRR 求解算法

### 算法选型

采用**二分法（Bisection Method）**求解，原因：
- 保证收敛（IRR 函数 NPV 在 (-1, +∞) 区间内单调）
- 实现简单、计算稳定
- 精度可控

### 算法步骤

```
输入: 现金流数组 cashflows[{offset, amt}]
输出: 周期费率 R

Step 1: 设定搜索边界
        lo = -0.999     (R 必须 > -1，否则 (1+R) 为负导致幂运算异常)
        hi = 10         (初始上界 1000%，足够覆盖所有正常场景)

Step 2: 二分迭代（最多 300 次）
        while hi - lo 未收敛:
          mid = (lo + hi) / 2
          NPV_mid = Σ amt_t / (1 + mid)^offset_t
          if NPV_mid * NPV_lo <= 0:  // 根在 [lo, mid] 之间
            hi = mid
          else:                       // 根在 [mid, hi] 之间
            lo = mid

Step 3: 返回 (lo + hi) / 2

精度: 当 hi - lo < 1e-16 时停止
最大迭代: 300 次（实际收敛通常在 50-100 次）
```

### 边界扩展

若初始 `hi = 10` 时 NPV(lo) 和 NPV(hi) 同号（说明根不在初始区间内），自动扩展上界：
```
hi = 100 → 1000 → 10000
```

这种扩展仅在异常场景触发（如所有现金流同号）。

### 精度验证

以央行文档例5（等额本息 + 期初费用）为基准：

| 指标 | 央行文档 | 本系统 | 偏差 |
|------|----------|--------|------|
| 月费率 R | 0.0792% | 0.0792% | < 0.0001pp |
| 年化率 12×R | 0.952% | 0.952% | < 0.001pp |

---

## 3.8 单利年化换算

本系统严格执行**单利**换算规则，参考银发〔2005〕129 号文件。

### 换算公式

$$
\text{年化率} = R_{\text{period}} \times n
$$

其中：
- $R_{\text{period}}$ = IRR 法求得的周期费率
- $n$ = 年内还款频率

| 单位周期 | n | 示例 |
|----------|---|------|
| 按月 | 12 | 月费率 0.0792% × 12 = 0.9504% |
| 按季 | 4 | 季费率 0.25% × 4 = 1.00% |
| 按年 | 1 | 年费率 1.01% × 1 = 1.01% |

### 复利 vs 单利（对比说明）

复利换算（非本系统采用）：
$$
\text{复利年化率} = (1 + R_{\text{period}})^n - 1
$$

单利换算（**本系统采用**）：
$$
\text{单利年化率} = R_{\text{period}} \times n
$$

> ⚠️ 两者差异在周期费率较大时明显。例如月费率 1%：单利 12% vs 复利 12.68%。企业融资成本测算**必须使用单利**，这是监管规定。

---

## 3.9 循环授信模式

对于一次授信循环使用额度的贷款业务，由于提款次数、时间、金额均不确定，费用无法精确分摊到每次提款。**简便合理的处理方式**是：以**授信额度和授信期限**作为测算的本金和期限。

### 测算参数映射

| 普通贷款参数 | 循环授信替代值 |
|-------------|---------------|
| 本金 A | 授信额度 |
| 期限 T | 授信期限 |
| 还款方式 | 到期一次性还本 + 按周期付息 |
| 费用年化率基准 | 同上，将授信视作一笔完整贷款 |

### 示例（央行文档例13）

| 条件 | 值 |
|------|----|
| 授信额度 | 100 万元 |
| 授信期限 | 2 年 |
| 担保费 | 1 万元（授信日一次性支付） |
| 还款计划 | 按月付息，到期还本 |

测算结果：担保费年化率 ≈ **0.50%**

---

## 3.10 边界条件处理

### 空费用列表

当用户未添加任何费用时：
- 费用年化率合计 = 0
- 综合融资成本年化率 = 贷款利息年化率

### 非整数周期

通过名义月差（30/360 体系）自动处理，支持任意日期组合。

### 超长期限（> 60 期）

还款计划表自动隐藏以避免页面过长。IRR 求解和年化率计算**完全不受影响**。

### 负年化率

IRR 解可能为负数（例如本金 > 还款总额的极端情况）。系统正常返回负数值，不做特殊拦截。

### 金额精度

系统内部所有计算使用 JavaScript 双精度浮点数（约 15-17 位有效数字）。显示时按场景截取：
- 年化率：默认 4 位小数（`0.0000%`）
- 单位周期费率：6 位小数（`0.000000%`）
- 金额：2 位小数或整数元

### 求解失败的边界情况

| 场景 | 表现 | 处理 |
|------|------|------|
| 所有现金流同号（无本金流入） | NPV 无根 | 二分法会扩展 hi 到 10000 后返回最接近 0 的值，此时 IRR 无实际意义 |
| 本金 = 0 | 现金流无效 | 输入校验阶段拦截 |
| 只有一期（N=1） | IRR 退化为代数解 | 二分法同样能正确收敛 |

---

## 3.11 计算结果验证方法

### 与央行文档示例对照

本系统已通过央行文档全部 13 个示例的验证：

| 示例 | 场景 | 期望 | 实测 | 偏差 |
|------|------|------|------|------|
| 1 | 一次性还本 + 期初手续费（整数） | 0.500% | 0.504% | < 0.01% |
| 2 | 一次性还本 + 期初担保费（非整数） | 1.014% | 1.016% | < 0.01% |
| 3 | 等额本金 + 期初保证保险（整数） | 0.967% | 0.967% | < 0.001% |
| 4 | 等额本金 + 期初保证保险（非整数） | 0.922% | 0.922% | < 0.001% |
| 5 | 等额本息 + 期初保证保险（整数） | 0.952% | 0.952% | < 0.001% |
| 6 | 等额本息 + 期初中介费（非整数） | 0.929% | 0.929% | < 0.001% |
| 7 | 一次性还本 + 周期担保费（期初） | 1.010% | 1.010% | < 0.001% |
| 8 | 一次性还本 + 周期监管费（非整数） | 1.009% | 1.010% | < 0.001% |
| 9 | 等额本金 + 周期保证保险（期初） | 4.559% | 4.559% | < 0.001% |
| 10 | 等额本金 + 周期保证保险（非整数） | 4.354% | 4.353% | < 0.001% |
| 11 | 等额本息 + 周期代理费（期末） | 4.476% | 4.477% | < 0.001% |
| 12 | 等额本息 + 周期代理费（非整数） | 4.369% | 4.370% | < 0.001% |
| 13 | 循环授信 + 期初担保费 | 0.500% | 0.503% | < 0.01% |

### 独立验算步骤

审计或二次核对时，可按以下步骤手动验算：

**Step 1**：确认本金 A、期数 N、周期利率 r

**Step 2**：生成本金序列 `P_t`
```
等额本息: C = A × r(1+r)^N / [(1+r)^N - 1]
          P_1 = C - A×r
          P_2 = C - (A-P_1)×r
          ...
          P_N = 最后剩余本金
等额本金: P_t = A/N
一次性还本: P_N = A, 其余 = 0
```

**Step 3**：计算每期 offset（名义周期偏移）

**Step 4**：构造费用现金流
```
期初: [(0, +A), (0, -F), (offset_1, -P_1), ..., (offset_N, -P_N)]
周期: [(0, +A), (offset_t, -(P_t + F_per)) ...]  (期末支付)
```

**Step 5**：用金融计算器或 Excel IRR 函数求解
```
Excel 公式: =IRR(现金流区域, 0.01)
注意: Excel IRR 默认按等间隔（offset=1,2,3...）计算
      对于非整数周期需用 XIRR
Excel XIRR: =XIRR(金额区域, 日期区域)
```

**Step 6**：单利换算 → 年化率 = R × n

### 交叉验证工具推荐

| 工具 | 能力 | 局限 |
|------|------|------|
| Excel `IRR` | 等间隔周期 IRR | 不支持非整数 offset |
| Excel `XIRR` | 支持任意日期 IRR | 日期必须是实际日历日，不是名义月差 |
| Python `numpy.irr` | 等间隔 IRR | 已废弃，用 `numpy_financial` |
| Python `numpy_financial.irr` | 等间隔 IRR | 需配合手动构造非等间隔场景 |
| Python `scipy.optimize.brentq` | 任意方程求根 | 适合二次开发验证 |

---

## 附录 A：参考规范

| 编号 | 文件 | 关键内容 |
|------|------|----------|
| 〔2021〕第 3 号 | 中国人民银行公告 | 贷款年化利率计算方法 |
| 银发〔2005〕129 号 | 人民银行 | 单利换算方式确认 |
| — | 企业贷款综合融资成本年化率计算规则及示例 | 13 个示例完整推导 |

## 附录 B：版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-08-13 | 初始发布，全部 13 个央行示例验证通过 |
