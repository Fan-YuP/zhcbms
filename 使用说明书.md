# 企业贷款综合融资成本明示系统 使用说明书

> 版本：v1.1
> 更新日期：2026-08-24
> 适用对象：银行业务人员（填写人）、企业客户（阅知人）、系统部署人员

---

## 目录

1. [项目简介](#1-项目简介)
2. [功能特性](#2-功能特性)
3. [环境要求](#3-环境要求)
4. [目录结构](#4-目录结构)
5. [安装步骤](#5-安装步骤)
6. [启动方法](#6-启动方法)
7. [配置说明](#7-配置说明)
8. [常用操作指南](#8-常用操作指南)
9. [计算规则说明](#9-计算规则说明)
10. [常见问题解答（FAQ）](#10-常见问题解答faq)
11. [故障排除](#11-故障排除)

---

## 1. 项目简介

本系统是按照人民银行关于明示企业贷款综合融资成本的要求开发的**纯前端网页工具**，用于填写并自动生成《企业贷款综合融资成本清单》。

系统在浏览器内完成全部计算：

- 依据《中国人民银行公告〔2021〕第 3 号》确定贷款年化利率口径；
- 依据《企业贷款综合融资成本年化率的计算规则及示例》，对利息外的每一项融资费用采用**内部收益率法（IRR）逐项计算**单位周期费率，并以**单利方式**（单位周期费率 × 年内周期数）换算为年化率；
- 自动汇总生成"企业综合融资成本合计"及"其中银行收取部分"。

系统**无需后端服务器、无需数据库**，填写的数据不离开本机，不上传、不保存，适合在银行内网环境部署使用。

> ⚠️ **重要提示**：本表仅作为明示企业综合融资成本的参考，不影响企业正常的贷款授信审批，不作为企业与银行之间相关贷款要素的确认或约定。

---

## 2. 功能特性

| 功能 | 说明 |
|------|------|
| 贷款信息填写 | 本金、年化利率、期限（日/月/年）、还款方式、还款周期、担保方式等 |
| 多种还款方式 | 等额本息、等额本金、按月结息到期利随本清、按月结息按计划表还本到期利随本清 |
| 自定义还本计划 | "按计划表还本"方式下可逐期录入还本金额，到期自动结清剩余本金 |
| 非整数首期支持 | 可填写贷款起始日、首次还款日、到期日，自动按 30/360 规则计算分数期偏移 |
| 费用逐项管理 | 预置 12 个常见费用行，可任意增删；支持一次性支付与周期性支付两类费用 |
| 周期费用精细化 | 支持支付周期（月/季/年）、支付时点（期初付/期末付）、每期金额设置 |
| 费用分担 | 支持企业/银行/其他（如政府减免）三方分担，按企业实际承担金额计算年化率 |
| 实时自动计算 | 任意输入修改后自动重算，无需点击计算按钮 |
| 填表校验 | 打印/导出前自动校验必填项、格式、费用分担闭合、支付周期一致性等业务规则 |
| 个人经营性贷款 | 勾选个体工商户/小微企业主后，企业类型、企业性质栏自动禁用（填表说明4） |
| 导出 PDF | 一键将清单导出为 A4 横向 PDF 文件 |
| 打印 | 调用浏览器打印，自动隐藏操作按钮与校验标记 |
| 导出 Excel 样表 | 下载空白《附1：企业贷款综合融资成本清单》Excel 模板 |

---

## 3. 环境要求

### 3.1 使用方（业务人员电脑）

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 7/10/11、macOS、主流 Linux 桌面系统 |
| 浏览器 | **推荐** Chrome 90+ 或 Edge 90+（Windows 自带）；兼容 Firefox 88+、Safari 14+ |
| 网络 | 通过内网地址访问部署好的系统即可；**无需连接互联网** |
| 屏幕分辨率 | 建议 1280×720 以上 |

> ⚠️ 不建议使用微信内置浏览器打开，可能导致 PDF 下载被拦截。

### 3.2 部署方（服务器）

| 项目 | 最低要求 |
|------|----------|
| 操作系统 | Windows / Linux 均可 |
| Web 服务 | 任选其一：Nginx、IIS、Apache、Python 内置 http.server |
| 磁盘空间 | 20 MB 以上 |
| 其他 | 无需数据库、无需安装 Java/.NET/PHP 等运行环境 |

---

## 4. 目录结构

部署所需的**最少文件**如下（其余文件为开发验证用，不影响运行）：

```
综合融资成本明示/
├── index.html                          ★ 主页面（界面 + 全部计算逻辑，必读）
├── lib/                                ★ 第三方库目录（必须保留）
│   ├── html2canvas.min.js              页面截图引擎（PDF 导出用）
│   └── jspdf.umd.min.js                PDF 生成引擎
├── 附1：企业贷款综合融资成本清单(1).xlsx   ★ Excel 空白样表（"导出Excel样表"按钮使用）
│
├── 使用说明书.md                        本文档
├── 项目文档.md                          项目技术与部署文档
├── Nginx部署手册.md                     Nginx 详细部署手册
├── 附2：企业贷款综合融资成本年化率的计算规则及示例.doc   监管计算规则原文
├── verify_all_examples.js              开发验证：央行13个示例核对脚本
├── verify_index_calc.js                开发验证：端到端计算回归脚本
└── test_ind_loan.js                    开发验证：个人贷款计算脚本
```

> ⚠️ **部署三要素**：`index.html`、`lib/` 目录、`附1：...xlsx` 样表文件，三者缺一不可。缺少 `lib/` 会导致 PDF 导出失败；缺少 xlsx 会导致"导出Excel样表"按钮报错。

---

## 5. 安装步骤

### 5.1 获取项目文件

1. 将项目文件夹「综合融资成本明示」**完整复制**到目标服务器。
2. 逐一对照第 4 节目录结构，确认以下三项存在：
   - `index.html`
   - `lib/html2canvas.min.js`、`lib/jspdf.umd.min.js`
   - `附1：企业贷款综合融资成本清单(1).xlsx`

> 💡 整个系统是"绿色软件"，复制即完成安装，无需执行任何安装程序。

### 5.2 放置到 Web 服务目录

根据选用的 Web 服务，将文件夹放到对应位置：

| Web 服务 | 建议放置路径 |
|----------|-------------|
| Nginx (Linux) | `/var/www/loan-cost/` |
| Nginx (Windows) | `nginx安装目录/html/loan-cost/` |
| IIS | `C:\inetpub\wwwroot\loan-cost\`（或自定义物理路径） |
| Python http.server | 任意目录，启动时进入该目录即可 |

> ⚠️ **路径建议**：部分 Web 服务对中文路径支持不佳，建议将文件夹重命名为英文（如 `loan-cost`）后再部署。

---

## 6. 启动方法

### 6.1 方式一：Nginx（推荐用于正式环境）

1. 编辑 Nginx 配置文件（如 `conf/nginx.conf`），在 `http {}` 内加入：

```nginx
server {
    listen       8080;
    server_name  _;

    root   /var/www/loan-cost;    # 指向项目文件夹（含 index.html 的目录）
    index  index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
```

2. 检查配置并启动/重载：

```bash
nginx -t              # 检查配置语法
nginx                 # 启动（已运行则执行：nginx -s reload）
```

3. 浏览器访问 `http://服务器IP:8080/index.html`。

> 💡 更详细的 Nginx 配置（含 HTTPS、多端口、负载均衡）请参阅项目内《Nginx部署手册.md》。

### 6.2 方式二：IIS（Windows Server）

1. 打开「IIS 管理器」→ 右键「站点」→「添加网站」。
2. 填写：
   - 网站名称：`loan-cost`（任意）
   - 物理路径：项目文件夹所在路径
   - 端口：`8080`（或任一空闲端口）
3. 点击「确定」后，浏览器访问 `http://服务器IP:8080/index.html`。

### 6.3 方式三：Python http.server（临时测试用）

1. 安装 Python 3（服务器上若已有可跳过）。
2. 打开命令行，进入项目目录：

```bash
cd /d F:\综合融资成本明示        # Windows 示例
# cd /var/www/loan-cost        # Linux 示例
```

3. 启动服务：

```bash
python -m http.server 8080 --bind 0.0.0.0
```

4. 看到 `Serving HTTP on 0.0.0.0 port 8080 ...` 即启动成功，访问 `http://服务器IP:8080/index.html`。
5. 停止服务：在命令行窗口按 `Ctrl + C`。

### 6.4 方式四：直接双击打开（单机使用）

不部署服务器时，可直接双击 `index.html` 用浏览器打开使用，全部计算功能正常。

> ⚠️ 直接双击打开时，"导出Excel样表"按钮在个别浏览器中可能因安全策略无法下载，建议通过 HTTP 方式访问。

### 6.5 防火墙放行（局域网访问）

若其他电脑无法访问，请在服务器上放行端口（Windows 示例，管理员权限运行）：

```powershell
netsh advfirewall firewall add rule name="综合融资成本明示" dir=in action=allow protocol=TCP localport=8080
```

---

## 7. 配置说明

本系统**开箱即用，无需任何配置文件**。所有选项均在页面上操作。以下为页面各配置项的含义与填写口径。

### 7.1 一、企业贷款利率（基本信息区）

| 字段 | 必填 | 填写说明 |
|------|:----:|----------|
| 银行名称 | ✅ | 填写银行全称 |
| 贷款本金（万元） | ✅ | 单位**万元**，如 100 万元填 `100`，最多 2 位小数 |
| 贷款年化利率（%） | ✅ | 百分数，如 4.75% 填 `4.75`，最多 4 位小数 |
| 担保方式 | ✅ | 勾选 信用/保证/抵押/质押 至少一项，或在"其他"栏填写具体说明 |
| 企业/借款人名称 | ✅ | 企业填全称；个人经营性贷款填借款人姓名 |
| 贷款期限（日/月/年） | ✅ | 整数 + 单位下拉框，如 `24` + `月`、`657` + `日` |
| 还款方式 | ✅ | **无默认选项**，需手动四选一，详见 7.2 |
| 还款周期 | 建议 | 文本，填 `按月`/`按季`/`按年`/`到期一次`，决定费用年化率的**单位周期** |
| 企业类型 | 条件必填 | 大/中/小/微型选一项；勾选"个人经营性贷款"后**不需填写**（自动禁用） |
| 企业性质 | 条件必填 | 国有控股/非国有控股选一项；同上自动禁用 |
| 个人经营性贷款 | 否 | 个体工商户 / 小微企业主，如实勾选 |
| 还款日期（起始日/首次还款日/到期日） | 否 | 选择"等额本金/等额本息/按月结息到期利随本清"时**自动显示**；仅当首次还款日 ≠ 起始日 + 1 个周期时需要填写，用于非整数首期精确计算；**本行为计算辅助栏，打印/导出 PDF 时不输出** |

### 7.2 还款方式对照

| 还款方式 | 每期现金流 | 适用场景 | 附加操作 |
|----------|-----------|----------|----------|
| 等额本息 | 每期还款额固定 | 最常见的中长期贷款 | — |
| 等额本金 | 每期还固定本金 + 递减利息 | 希望总利息较少 | — |
| 按月结息到期利随本清 | 按月付息，到期一次性还本 | 短期流动资金贷款 | — |
| 按月结息按计划表还本到期利随本清 | 按月付息，按自定义计划还本 | 约定分期还本计划的贷款 | 选择后自动展开**还本计划表**，逐行录入"期数 + 还本金额"，到期未安排完的余额自动在最后一期结清；该表仅为录入辅助，**打印/导出 PDF 时不输出** |

> 💡 **循环授信业务**：无专门选项。按监管规则以"授信额度"为本金、"授信期限"为期限，还款方式选"按月结息到期利随本清"录入即可（与央行文档例13 口径一致）。

### 7.3 二、企业贷款其他融资成本（费用明细区）

系统预置 12 行常见费用（银团贷款费用、贸易融资手续费、担保费、保险费、质押监管费、评估费、质押或抵押登记费、公证费、供应链平台服务费、政府应急转贷费等），可点击行尾"删"按钮删除，或点下方"＋添加费用项"新增。

每行字段：

| 字段 | 填写说明 |
|------|----------|
| 收费方 | 银行（银行填写）/ 第三方（银行承担）/ 第三方（企业填写） |
| 项目序号 | 按样表顺序填写 |
| 成本项目名称 | 如：担保费、评估费 |
| 是否涉及该项费用 | 勾选"是"后该行才参与计算与校验 |
| 收费机构名称 | 实际收费的单位名称 |
| 收费总额（元） | 该费用全部金额，单位**元** |
| 企业实际承担金额（元） | 企业自己支付的部分（年化率按此计算） |
| 银行承担金额（元） | 银行代为承担的部分 |
| 其他承担金额（元） | 如政府减免等 |
| 支付方式 | 一次性支付 / 周期性支付 / 其他方式 |
| 支付周期 | 周期性费用必填：月 / 季 / 年 |
| 支付时点 | `期末付`＝每期期末随还款支付；`期初付`＝每期期初预付下一期（首期在提款日） |
| 每期金额（元） | 周期性费用必填；一次性费用也可直接在此填写承担额度 |
| 融资成本年化率 | **自动计算**，无需填写 |
| 备注 | 特殊情况说明 |

> ⚠️ **三个金额的关系**：企业实际承担 + 银行承担 + 其他承担 **必须等于** 收费总额，否则打印/导出时会被校验拦截。

> 💡 **一次性费用的两种填法等效**：在"企业实际承担金额"列填写，或在"每期金额"列填写（填表说明11），系统均可正确计算；若两列都填，则必须一致。

### 7.4 三、企业综合融资成本合计（结果区）

- **企业综合融资成本合计** = 贷款年化利率 + 企业实际承担的逐项费用年化率之和（自动计算）。
- **其中，银行收取部分** = 贷款年化利率 + 收费方为"银行"的费用项年化率之和（自动计算）。

---

## 8. 常用操作指南

### 8.1 完整填表流程（标准贷款）

1. 打开系统页面。
2. 填写"一、企业贷款利率"区：银行名称、本金、年化利率、担保方式、借款人名称、期限、还款方式、还款周期。
3. 如是小微企业主/个体工商户贷款，勾选"个人经营性贷款"（企业类型、性质自动禁用）。
4. 在"二、企业贷款其他融资成本"区逐行处理费用：
   1. 不涉及的行保持"否"勾选（或删除该行）；
   2. 涉及的行勾选"是"，填写收费总额、三方分担金额、支付方式；
   3. 周期性费用再选支付周期（月/季/年）、支付时点（期初付/期末付）、每期金额。
5. 核对"三、企业综合融资成本合计"自动计算结果。
6. 点击工具栏**打印**或**导出PDF**：
   - 若弹出校验窗口，按提示逐项修正后重试；
   - 校验通过后打印或保存 PDF。
7. 打印件由客户经办人签字、银行签字并加盖业务章，填写填表日期。

### 8.2 操作示例：100 万等额本金贷款 + 期初保证保险费

1. 贷款本金填 `100`，年化利率填 `4.75`，期限 `24` 月，还款方式选"等额本金"，还款周期填 `按月`。
2. 找到"保险费"行，勾选"是"。
3. 收费总额填 `10000`，企业实际承担金额填 `10000`。
4. 支付方式选"一次性支付"。
5. 该行"融资成本年化率"自动显示约 `0.97%`，合计自动更新。

### 8.3 操作示例：按月支付的银团代理费（非整数首期）

1. 还款方式选"等额本息"，起始日填 `2024-01-10`，到期日填 `2026-01-10`。
2. 首次还款日填 `2024-02-20`（系统自动按 30/360 计算分数期偏移）。
3. "银团贷款费用"行勾选"是"，收费总额填 `48000`，企业实际承担填 `48000`。
4. 支付方式选"周期性支付"，支付周期选"月"，支付时点选"期末付"，每期金额填 `2000`。
5. 年化率自动显示约 `4.37%`。

### 8.4 使用还本计划表

1. 还款方式选择"按月结息按计划表还本到期利随本清"。
2. 页面自动展开"还本计划表"，点击"＋添加还本期"。
3. 逐行输入期数（如 `6`）和该期还本金额（元）。
4. 右侧提示文字实时显示"已安排还本 X 万元，到期前剩余 Y 万元将在最后一期结清"。

### 8.5 重置表单

点击工具栏**重置表单**，确认后清空全部填写内容并恢复预置费用行；还款方式恢复为"请选择"未选状态。

### 8.6 填充测试数据（临时功能）

点击工具栏黄色按钮**填充测试数据（临时）**，系统每次随机生成一套内部自洽的测试数据（本金 50~2000 万元、利率 3%~8%、期限 6~60 个月、还款方式随机、首期可能非整月，并随机抽取 1~3 项一次性或周期性费用），并弹出本次填充的要素摘要与当前综合融资成本合计，供核对页面计算结果。

> ⚠️ 该按钮为上线前验证用的**临时功能**，正式投入使用前应从工具栏移除（删除 `index.html` 工具栏中 `fillTestData()` 对应按钮及脚本中同名函数即可）。

---

## 9. 计算规则说明

供核对结果时参考（详细推导见《附2：企业贷款综合融资成本年化率的计算规则及示例.doc》）：

1. **综合融资成本年化率** = 贷款年化利率 + Σ（逐项费用年化率）。
2. **费用年化率**：对每项费用独立构造现金流（本金流入、费用流出、本金偿还流出），用内部收益率法（IRR）解出单位周期费率，再按**单利**换算（× 年内周期数）。
3. **单位周期**：出现次数最多的相邻两次计息间隔（月/季/年），最长不超过 1 年；到期一次性业务期限超过 1 年时单位周期为 1 年，不足 1 年时为整个期限。
4. **非整数周期**：贴现因子为 `(1 + ft×R)×(1+R)^st`，其中 `st` 为完整周期数、`ft` 为小数部分（按 30/360 规则计算）。
5. **期初付费用**：首期费用在提款日支付（offset=0），对应担保费、保险费等预付情形；**期末付费用**随每期还款一并支付，对应代理费等情形。
6. 系统计算结果已通过央行文档全部 13 个示例验证（偏差均小于 0.01 个百分点，属文档舍入范围）。

---

## 10. 常见问题解答（FAQ）

**Q1：输入后结果没有变化？**
A：本系统为实时自动计算，正常情况下输入即更新。请确认输入的是纯数字（不要带逗号、空格、单位文字），并查看页面顶部是否有红色校验标记。

**Q2：某项费用年化率显示 0.00%？**
A：请检查该行：① 是否勾选"是"；② "企业实际承担金额"是否为 0 或为空（一次性费用也可改填在"每期金额"列）。银行承担的部分不计入企业成本，显示 0.00% 属正常。

**Q3：打印时弹窗提示"费用分担三项之和应等于收费总额"？**
A：该费用行的 企业实际承担 + 银行承担 + 其他承担 相加必须等于收费总额。若费用全部由企业承担，只在"企业实际承担金额"填与收费总额相同的数即可。

**Q4：弹窗提示"支付周期共 N 期 × 每期金额 与 企业实际承担金额不一致"？**
A：按填表说明11，支付周期期数 × 每期金额 应等于企业实际承担总金额。请核对：期限推算的支付次数 N、每期金额、承担总额三者，修正任意一项使其闭合。

**Q5：担保费是提款时一次性扣收的，支付时点选什么？**
A：支付方式选"一次性支付"，无需选择支付时点。"期初付/期末付"仅用于周期性支付的费用。

**Q6：保险费每月预付下一个月的，怎么填？**
A：支付方式选"周期性支付"，支付周期选"月"，支付时点选"**期初付**"，每期金额填每月保费。

**Q7：企业类型/企业性质为什么不能勾选了？**
A：您勾选了"个人经营性贷款（个体工商户/小微企业主）"。按填表说明4，此时这两栏不需填写，系统自动禁用；取消勾选即可恢复。

**Q8：循环授信（随借随还）业务怎么填？**
A：还款方式选"按月结息到期利随本清"，本金填授信额度，期限填授信期限，费用按实际支付方式填写。

**Q9：金额单位是什么？**
A：贷款本金单位为**万元**；所有费用金额单位均为**元**。注意区分，避免差 1 万倍。

**Q10：贷款年化利率填 4.75 还是 0.0475？**
A：填 `4.75`（百分数形式）。

**Q11：可以两台电脑同时用吗？**
A：可以。系统无并发限制，部署在服务器后，局域网内任意多台电脑同时访问互不影响；各浏览器中的填写内容相互独立。

**Q12：填写的内容会保存吗？**
A：不会。系统不存储任何数据，刷新或关闭页面后内容清空。请及时打印或导出 PDF 留存。

---

## 11. 故障排除

### 11.1 页面打不开（浏览器显示无法访问）

1. 确认服务器 Web 服务已启动（Nginx/IIS/Python 进程存在）。
2. 在服务器本机访问 `http://127.0.0.1:端口/index.html` 测试：
   - 本机能开、别的电脑不能开 → 防火墙问题，按 6.5 节放行端口；
   - 本机也打不开 → 检查端口是否被其他程序占用（Windows 执行 `netstat -ano | findstr 8080`），或配置文件中的 root 路径是否正确。
3. 确认 URL 中包含文件名 `index.html`，且大小写一致（Linux 区分大小写）。

### 11.2 点击"导出PDF"无反应或报错

1. 检查浏览器控制台（按 `F12` → Console）是否有红色报错。
2. 确认 `lib/` 目录已随项目完整部署，且能直接访问：
   - 浏览器打开 `http://服务器IP:端口/lib/html2canvas.min.js`，应显示一大段代码而非 404。
3. 确认是通过 `http://` 访问的页面（而非微信内置浏览器）。
4. 若提示校验错误，说明表单存在不符合填表说明的内容，按弹窗提示修正后再导出。

### 11.3 点击"导出Excel样表"报错

1. 确认 `附1：企业贷款综合融资成本清单(1).xlsx` 与 `index.html` 在**同一目录**。
2. 确认是通过 `http://` 方式访问（直接双击打开页面时部分浏览器会拦截下载）。

### 11.4 打印/PDF 中样式异常

1. 请使用 Chrome 或 Edge 浏览器打印。
2. 打印对话框中纸张方向选**横向**，边距选"默认"或"无"。
3. 中文显示为方框：操作系统缺少中文字体，安装宋体/微软雅黑后重试。

### 11.5 计算结果与手工测算略有差异（< 0.01 个百分点）

属正常现象，常见原因：

1. 监管文档示例公布值本身只保留 2~4 位小数，存在舍入；
2. 非整数首期未填写还款日期（系统在"还款日期"行填写起始日/首次还款日/到期日后可精确到分数期）；
3. 多次支付且金额不等的周期费用，系统按"每期金额"等额处理（可在备注中说明）。

### 11.6 页面显示错乱、按钮不响应

1. 确认浏览器版本符合要求（Chrome/Edge 90+），不要使用 IE 浏览器。
2. 强制刷新页面：`Ctrl + F5`（清除缓存）。
3. 确认 `index.html` 文件完整未被修改（可用原始部署包覆盖恢复）。

---

> 📎 **相关文档**：《项目文档.md》（技术与部署细节）、《Nginx部署手册.md》（Nginx 专项）、《附2：企业贷款综合融资成本年化率的计算规则及示例.doc》（监管规则原文）。
